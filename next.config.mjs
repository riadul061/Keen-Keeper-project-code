/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
   images: {
    domains: ["randomuser.me","i.pravatar.cc"],
  },
};

export default nextConfig;
